// Question: Create an array called colors containing the names of three different colors. Print each color to the console using a loop.



let colors = ["black", "green", "yellow"];

for (let a = 0; a < colors.length; a++) {
    console.log(colors[a]);
}
